/** Route: /operator/create-trip — new trip form. */
import CreateTripScreen from '@/screens/operator/CreateTripScreen';

export default function OperatorCreateTripRoute() {
  return <CreateTripScreen />;
}
