/** Route: /operator/vehicle-edit?id= — edit an existing vehicle. */
import VehicleEditScreen from '@/screens/operator/VehicleEditScreen';

export default function OperatorVehicleEditRoute() {
  return <VehicleEditScreen />;
}
