/** Route: /operator/vehicles — fleet list. */
import VehicleListScreen from '@/screens/operator/VehicleListScreen';

export default function OperatorVehiclesRoute() {
  return <VehicleListScreen />;
}
