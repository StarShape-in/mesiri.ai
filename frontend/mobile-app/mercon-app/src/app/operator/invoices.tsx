/** Route: /operator/invoices — invoice list. */
import InvoiceListScreen from '@/screens/operator/InvoiceListScreen';

export default function OperatorInvoicesRoute() {
  return <InvoiceListScreen />;
}
