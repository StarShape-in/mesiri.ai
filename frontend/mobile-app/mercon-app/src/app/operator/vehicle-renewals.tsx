/** Route: /operator/vehicle-renewals — vehicle document expiry tracker. */
import VehicleRenewalScreen from '@/screens/operator/VehicleRenewalScreen';

export default function OperatorVehicleRenewalsRoute() {
  return <VehicleRenewalScreen />;
}
