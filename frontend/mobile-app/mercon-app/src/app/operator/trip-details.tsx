/** Route: /operator/trip-details?id= — a single trip's detail view. */
import TripDetailsScreen from '@/screens/operator/TripDetailsScreen';

export default function OperatorTripDetailsRoute() {
  return <TripDetailsScreen />;
}
