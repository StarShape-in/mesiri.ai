/** Route: /operator/driver-edit?id= — edit an existing driver. */
import DriverEditScreen from '@/screens/operator/DriverEditScreen';

export default function OperatorDriverEditRoute() {
  return <DriverEditScreen />;
}
