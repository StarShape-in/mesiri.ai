/** Route: /operator/customer-edit?id= — create (no id) or edit (with id) a customer. */
import CustomerEditScreen from '@/screens/operator/CustomerEditScreen';

export default function OperatorCustomerEditRoute() {
  return <CustomerEditScreen />;
}
