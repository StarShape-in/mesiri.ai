/** Route: /operator/more — operator hub for Fleet, Invoices, Customers, Vehicle Renewals. */
import MoreScreen from '@/screens/operator/MoreScreen';

export default function OperatorMoreRoute() {
  return <MoreScreen />;
}
