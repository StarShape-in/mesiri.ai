/** Route: /trip/delivery — POD photo + confirm delivery (AtDelivery -> Completed). */
import DeliveryVerificationScreen from '@/screens/driver/DeliveryVerificationScreen';

export default function DeliveryRoute() {
  return <DeliveryVerificationScreen />;
}
