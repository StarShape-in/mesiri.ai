/**
 * Route entry for the Operator Home tab — implementation lives in the
 * dashboard feature module (src/features/dashboard).
 */
export { default } from '@/features/dashboard/screens/DashboardHomeScreen';
