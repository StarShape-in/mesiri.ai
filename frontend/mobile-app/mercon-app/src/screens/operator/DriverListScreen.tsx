/**
 * Route entry for the Operator Drivers tab — implementation lives in the
 * drivers feature module (src/features/drivers).
 */
export { default } from '@/features/drivers/screens/DriversScreen';
